```code-script
export function markdown() {
  const message = 'Require markdown';
  new Notice(message);
  console.log(message);
}
```

```code-script
// codeScriptName: namedScriptBlock
export function markdownWithNamedScriptBlock() {
  const message = 'Require markdown with named script block';
  new Notice(message);
  console.log(message);
}
```
